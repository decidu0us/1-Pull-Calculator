# 抽卡计算器 · 交付概览（v3 精简优化版）

> 最后更新：2026-08-04 · 优化范围：双版本 JS 代码精简

## 交付物
- **文件**：`抽卡计算器基础版.html`（支持上传背景） / `抽卡计算器固定背景版.html`（背景固定）
- 单文件、零依赖、离线可用

## 本次优化变更（v3 → v3精简版）

### Bug 修复（2026-08-04 下午）
- **崩铁跨周崩溃**：`computeDate()` 返回值属性为 `e` 而调用处取 `dt.end` → 崩铁计算崩溃、界面残留原神旧值。已改为返回 `end:en`，原神/崩铁警告逻辑（卡池已结束提示）同时修复
- **y 跨版本语义修正**：代码原为 `y=⌊(today−v0)/V⌋+1`（开始当天即 y=1），与文档公式不符。已改为跨版本数公式 **y=⌊(end−v0)/V⌋−⌊(today−v0)/V⌋**（未跨版=0）
- **用户场景验证**：today=start=2026-08-04 时，崩铁 h=4575（=20×150+3×225+1×900）与原神 h=3000（y=0 无跨版收益），符合预期
- 回归 7 场景通过：跨月/跨版本/下半/手动 b/未来卡池/已结束卡池

### 死代码移除
- 移除 `DEF.R=150`（公式硬编码每日60原石，R 从未被任何公式行引用）
- 移除 `LABEL.R`（对应标签从未在公式面板中渲染）
- 移除常量注释中的 R 项

### 代码去重（核心优化）
- 提取 `computeDate()` 共用函数：calcYS 与 calcSR 中 start/end/b/x/v0/y 计算完全重复（~360字节 → ~150字节共用函数）
- 两个计算函数从各 15 行日期计算 → 各 1 行调用

### 公式与备注
- 界面公式 `h=b·(60+y3)+c+d'` 与代码 `h=b*(60+y3)+c+de` 一致
- 所有跨月/跨版本/跨周备注统一
- 注释完整保留，仅更新了部分注释以反映代码结构变化

### 保留不变
- 水印（背景斜向大字 + 右下角徽标）
- 背景图片（内置 base64 申鹤图）
- 全部交互功能（上传背景/透明度/蒙版/公式编辑/游戏切换）
- 5.0 文件夹文件完全未碰

### 优化效果
- JS 验证：两个文件均通过 `new Function()` 语法检查 ✓
- 计算结果验证：computeDate 与原始内联代码输出完全一致 ✓
- 代码行数减少 ~15 行（日期计算完全去重）
- 内存占用：减少 DEF/LABEL 各 1 个死键，减少 2 处重复变量栈帧

## 界面结构
- 左右双栏：左侧输入区 **2/3**，右侧输出区 **1/3**（窄屏自动上下）
- 双模式切换：原神 / 崩铁（各自独立公式常量）
- 公式面板可显示/编辑全部系数，实时重算，一键恢复默认

## 交付物
- **文件**：`D:\ai\抽卡计数器\抽数计算器.html`（单文件、零依赖、离线可用）
- 另存旧版备份于 `D:\ai\抽卡计数器\1.0\`（用户侧留存）

## 界面结构
- 左右双栏：左侧输入区 **2/3**，右侧输出区 **1/3**（窄屏自动上下）
- 输入 a~g 一行两项；**剩余天数 b 单列一行**，附时钟 + 日历 + 上/下半切换
- 「显示 / 编辑公式」按钮在两栏下方**正中间**；下方为指定备注
- 水印（背景斜向大字号 + 右下角徽标）+ 页脚备注：本抽卡计算器由花瓷树开发，aigc率100%，感谢梁白开

## 输入项
| 项 | 说明 |
|---|---|
| a | 已抽多少抽 |
| b | 剩余天数（今天每日已完成，凌晨4点后更新日期；可日历自动算或手填） |
| c | 活动获得原石（**支持算式**，如 300+500×2，内置无 eval 解析器） |
| d | 拥有原石数 |
| e | 拥有星辉（备注：星芒/4=星辉） |
| f | 拥有限定抽 |
| g | 拥有常驻抽 |
| y0 | 纪行拿满开关：**默认开启**，开启时 y0=y−1、关闭时 y0=y（每版 +5 常驻抽） |
| y1 | 前瞻开关：距**上半卡池开始日期**（版本更新日）≤12 天**默认关闭**，关闭时 y1=y−1、开启时 y1=y（每版 300 原石） |
| y4 | 大月卡开关：**默认关闭**，关闭时 y4=0、开启时 y4=y−1（默认此版本已拿全奖励；每版 680 原石 + **4** 限定抽） |
| 上/下半 | 切换按钮；下半时上半卡池时间 = 下半开始 − 21 天（作为版本更新时间） |

## 核心规则
- **跨月 x**：今天 → 卡池结束（开始+21−1）跨过的月份数，未跨月为 0
- **跨版本 y**：每 V=42 天一版本，以上半卡池开始为版本更新日；y = ⌊(结束−v0)/42⌋ − ⌊(今天−v0)/42⌋（v0=上半开始，下半时=开始−21），未跨版为 0
- **跨月加成**：f +5x、g +5x、d +1600x（新老深渊更新）
- **跨版本加成**：g +5y0（纪行）、f +**4**y4（大月卡）、d +300y1 + 600y2 + 450y3 + 680y4（前瞻/更新/危战/大月卡；y2=y3=y；y0 开启时=y−1、关闭时=y；y1 关闭时=y−1、开启时=y；y4 关闭时=0、开启时=y−1，y−1 下限 0）
- **公式链**：b=（今天至开始+21）−1；h = b·150 + c + d′；i = ⌊h/160⌋ + ⌊(e+g′/5)/5⌋ + f′；j = ⌊i/25⌋；k = i+j；l = k+a
- **凌晨4点刷新**：每日刷新点为凌晨 4 点，4 点前仍计前一天（8月4日凌晨3点 = 8月3日），时钟每秒刷新、跨点自动重算

## 特色功能
- **公式显示&编辑**：面板逐条展示公式，全部 15 个系数（21/150/160/25/42/300/600/450/680 等）可改，实时重算，一键恢复默认
- **自定义背景**：页头「🎨 自定义背景」→ 上传图片自动拉伸铺满（cover）+ **图片透明度**滑块 + **界面透明度**滑块（面板半透明透出背景图，含毛玻璃模糊）+ 一键移除
  - **默认背景 = 申鹤（已 base64 烤入 HTML，网页自包含）**：原先依赖外部 `申鹤.jpg`，现已把图片以 `data:image/jpeg;base64,…` 直接写进 `BG_DEF`，网页单文件即可显示，可脱离外部文件
  - **记忆功能**：用户上传的图片（base64）与两份透明度写入 `localStorage`，下次打开自动沿用，无需重设；「移除背景」回落到内置申鹤背景
  - **📥 导出含背景网页**：点此按钮会把「当前生效背景 + 当前透明度」烤进 HTML 并下载为独立 `抽数计算器.html`（浏览器无法改写原文件，故用下载嵌入版实现「图片加进网页文件」）
- **可靠性**：四则解析器无 eval、除零兜底、非法输入标红、卡池已结束警示、日期用 UTC 差避免夏令时误差
- **代码优化（v3.1）**：HTML 去缩进空白紧凑化；CSS 全单行化；JS 提取 `syncVars()`/`setUI()` 去除重复
- **代码优化（v3.2）**：CSS 行尾自说明注释精简；JS 分隔注释缩短；HTML 属性引号去冗；体积 29,454 → 28,530（较 v3.1 再省 3.1%，较 2.0 省 7.2%），注释/水印/背景记忆全保留，回归 6/6
- **图片存储（v3.3）**：默认背景申鹤以 base64 烤入 HTML（网页自包含，不再依赖外部 `申鹤.jpg`）；新增「📥 导出含背景网页」按钮，可把当前自定义背景+透明度一起烤进独立 HTML 下载；JS 语法校验 + 嵌入/导出逻辑静态核对通过

## 验证记录（Node 离线验算全部通过）
1. 上半·同日开始：x=0, y=0, h=4600, k=36, l=46 ✓
2. 上半·跨月跨版本（开始 9/1，y1 开）：x=1, y=1, d′=2950, h=9250, k=64 ✓
3. 下半·跨版本（y4 开）：v0=开始−21, d′=3330（含大月卡 680）, f′=10, h=9630, k=72 ✓
4. 版本更新日当天：y=0 ✓；y1 默认规则（以 v0 为基准，≤12 天关 / >12 天开）✓
5. y1/y4 折算（v2.1）：y=2 全关→y1=1、y4=0；全开→y1=2、y4=1；y=1 y1关→0；y=0 防负 clamp ✓
5. 凌晨4点边界（03:00→前一天 / 04:00→当天 / 跨月 / 跨年）✓

## 备注
本抽卡计算器由花瓷树开发，aigc率100%，感谢梁白开。
